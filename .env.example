# Cloudflare Worker URL (deployed via wrangler)
CLOUDFLARE_WORKER_URL=https://venturo-img-gen.<subdomain>.workers.dev

# API key for Worker auth (generate your own secret)
CLOUDFLARE_API_KEY=your-worker-api-key

# Output canvas dimensions (default)
CANVAS_WIDTH=1400
CANVAS_HEIGHT=1024